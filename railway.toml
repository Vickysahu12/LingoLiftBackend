[build]
builder = "NIXPACKS"

[deploy]
startCommand = "PATH=/opt/venv/bin:$PATH uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3